[build]
builder = "dockerfile"

[deploy]
numReplicas = 1
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5

[[deploy.ports]]
port = 7474
protocol = "HTTP"

[[deploy.ports]]
port = 7687
protocol = "TCP"
