# FastAPI Backend Dependencies
# ============================

# Web Framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# HTTP & Auth
httpx>=0.26.0
python-jose[cryptography]>=3.3.0

# Environment
python-dotenv>=1.0.0

# AI/LLM (BYOK support)
anthropic>=0.18.0

# Database
neo4j>=5.14.0

# ML Inference (CPU-only for API server)
# Note: torch is installed separately in Dockerfile for CPU-only version
scikit-learn>=1.3.0
lightgbm>=4.0.0
joblib>=1.3.0
numpy>=1.24.0
pandas>=2.0.0
